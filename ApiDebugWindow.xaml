<Window x:Class="OpenMeido.ApiDebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="API调试工具" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="API调试工具" FontSize="16" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <!-- 输入区域 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <Label Content="测试消息:"/>
            <TextBox x:Name="TestMessageTextBox" Text="Hello, this is a test message." 
                     Padding="5" Margin="0,0,0,10"/>
            
            <StackPanel Orientation="Horizontal">
                <Button x:Name="SendTestButton" Content="发送测试请求" 
                        Padding="10,5" Margin="0,0,10,0" Click="SendTestButton_Click"/>
                <Button x:Name="TestConnectionButton" Content="测试连接" 
                        Padding="10,5" Margin="0,0,10,0" Click="TestConnectionButton_Click"/>
                <Button x:Name="ClearLogButton" Content="清空日志" 
                        Padding="10,5" Click="ClearLogButton_Click"/>
            </StackPanel>
        </StackPanel>
        
        <!-- 日志显示区域 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="LogTextBox" 
                     IsReadOnly="True" 
                     TextWrapping="Wrap" 
                     FontFamily="Consolas" 
                     FontSize="10"
                     Background="#F5F5F5"
                     Padding="10"/>
        </ScrollViewer>
        
        <!-- 状态栏 -->
        <Border Grid.Row="3" Background="#F0F0F0" Padding="10" Margin="0,10,0,0">
            <TextBlock x:Name="StatusTextBlock" Text="就绪" FontWeight="Bold"/>
        </Border>
    </Grid>
</Window>
